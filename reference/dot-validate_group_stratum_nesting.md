# Test if each group is assigned to exactly 1 stratum. Error otherwise.

Test if each group is assigned to exactly 1 stratum. Error otherwise.

## Usage

``` r
.validate_group_stratum_nesting(group_def, strata_def)
```

## Arguments

- group_def:

  groups of data

- strata_def:

  assigned stratum to group
