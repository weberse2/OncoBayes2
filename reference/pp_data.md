# Internal function to simulate from the posterior new parameter draws

Internal function to simulate from the posterior new parameter draws

## Usage

``` r
pp_data(object, newdata, draws, re.form)
```
