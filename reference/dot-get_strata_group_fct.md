# extracts from a blrmfit object and a given data-set the group and stratum factor

extracts from a blrmfit object and a given data-set the group and
stratum factor

## Usage

``` r
.get_strata_group_fct(object, data)
```
